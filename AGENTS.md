# 프로젝트 에이전트 지침 (바이브 코딩 기말과제 특화)

이 저장소에서 작업하는 모든 AI 에이전트(당신 포함)는 아래의 지침을 항상 최우선으로 준수해야 합니다. 
이 지침은 [게임알고리즘(3A)] 및 [게임최적화기법(2A)] 기말과제 제출 요구사항을 완벽히 충족하기 위한 기본 원칙입니다.

## 1. 필수 디렉토리 구조 준수
어떠한 개발/수정 작업이든 아래의 지정된 폴더 구조 안에서 이루어져야 합니다:
- `project/src/` : 실제 실행 코드 (메인 게임 로직 및 시스템)
- `project/tests/` : 테스트 및 검증 코드 (로직 테스트 슈트)
- `docs/` : 과목별 보고서 템플릿 및 아키텍처/설계 문서
- `worklogs/` : 트러블슈팅 및 작업 회고 로그 (가장 중요함)
- `README.md` 및 `run.bat` : 프로젝트 메인 진입 및 안내서

## 2. 작업 과정 로깅 (Troubleshooting Log) - ⭐ 가장 중요
단순히 코드를 작성하고 끝나는 것이 아니라, **문제 해결 과정**을 반드시 문서화해야 합니다.
- **언제?** 버그가 발생하거나, 새로운 프롬프트/지시를 받아 중요한 구조를 수정했을 때.
- **어디에?** `worklogs/troubleshooting_log.md` 파일의 최하단에 누적하여 기록합니다.
- **무엇을?** 
  1. 문제 발생 원인
  2. 사용자가 지시한 내용 (어떤 프롬프트가 입력되었는지)
  3. AI가 도출한 해결 방안 및 기술적 근거
  4. 테스트 검증 결과

## 3. 코드 작성 및 구조화 원칙
- **언어 및 환경**: Lua 5.1 (Love2D 11.5 호환)
- **모듈화**: 코드는 작고 재사용 가능한 모듈로 분할하며, `_G` 전역 공간 오염을 절대 금지합니다.
- **관심사 분리**: 데이터 연산 로직(`update`)과 화면 렌더링 로직(`draw`)을 철저히 분리하세요.
- **안정성 검증**: 핵심 기능(길찾기, 인벤토리, 충돌 등) 수정 시, `project/tests/` 내부의 테스트 하네스에도 해당 기능을 점검하는 코드를 함께 추가하여 무결성을 입증해야 합니다.

## 4. 보고서 템플릿 현행화
- 게임 로직 개선, 알고리즘 최적화, 혹은 기능 구현이 완료될 경우 `docs/report_3A_게임알고리즘.md` 의 '품질 개선 내용' 파트를 갱신해 주세요.
- 유용한 프롬프트 전략이나 인상적인 디버깅 과정이 있었다면 `docs/report_2A_게임최적화기법.md` 의 '시행착오와 개선 포인트'에 요약해서 추가해 주세요.

## 5. 실행 및 테스트 명세
- 테스트 실행 방법: 콘솔에서 `"love-11.5-win64/lovec.exe" "project/tests"` 실행 (또는 이에 준하는 task)
- 메인 프로젝트 실행: 루트 디렉토리의 `run.bat` 실행
