# 작업 로그 (Troubleshooting & 회고)

## [날짜 입력] 예시: 성능 최적화 이슈
- **문제 발생**: 오브젝트의 동시다발적 생성/삭제 시 메모리 단편화 및 프레임 저하 현상.
- **AI 지시 내용**: "`table.remove()`를 이용한 반복문에서 렉이 발생해. 이를 해결할 방법(오브젝트 풀링 등)을 적용해 줘."
- **해결 방안 도출**: AI가 O(1) Swap & Pop 삭제 기법 및 오브젝트 재사용 구조(Pooling) 제안 및 구현.
- **검증/결과**: 코드 반영 후 데모 실행 결과 프레임 저하 없이 안정적인 60FPS 방어 확인.

---

## 2026-06-06: 익스트랙션 긴장감 요소 (귀환 주문서/저주받은 유물) 추가
- **문제 발생**: 로그라이크 진행 과정이 다소 밋밋하여 텐션을 높일 기믹 필요.
- **AI 지시 내용**: "귀환에 3턴이 걸리고 피격시 취소되는 귀환 주문서와, 장착 시 해제 불가능한 하이 리스크 유물을 추가하라."
- **해결 방안 도출**: `main.lua` 내 턴 진행 로직과 데미지 판정 로직(`takeDamage`)에 `channeling_return` 변수를 연동하여 3턴 귀환 및 피격 취소 구현. `item.lua`의 `Item.new`에 `cursed` 속성을 추가하고 장비 해제 시 `gameState ~= "town"` 조건으로 차단.
- **검증/결과**: 구현 및 반영 완료. 사용자가 `run.bat`을 실행해 인게임 검증 진행 예정.

---

## 2026-06-07: 맵 생성기 개선 및 상호작용 지형(용암, 물, 풀 등) 추가
- **문제 발생**: 기존 맵이 단조로운 벽과 바닥으로만 이루어져 있어 생동감과 던전 탐험의 깊이가 부족함.
- **AI 지시 내용**: "맵이 좀 더 다양한 지형과 다양한 색을 지니고 던전 같은 형태면 좋겠어." (상호작용 가능한 방식 채택)
- **해결 방안 도출**: Cellular Automata 알고리즘 기반의 `generateSpecialTerrain()` 함수를 구현해 자연스러운 군집 지형(물, 용암, 풀, 흙)을 형성. `TILE_LAVA` 위에서는 매 턴 데미지를 받고, `TILE_WATER`에서는 회피율이 15% 깎이는 상호작용(Interactive Hazard) 로직을 `advanceTurn`과 전투 공식에 연동.
- **검증/결과**: 거대한 맵에서도 플레이어 기준 렌더링 최적화, 바이옴 변화에 따른 비주얼/로직 정상 분리 확인.

---

## 2026-06-07: 종족 및 직업별 특성(Skill Tree) 시스템 전면 도입 (126종)
- **배경/프롬프트**: 사용자가 "각 종족과 직업별로 1~3티어 스킬을 3개씩(총 18개) 구성해 달라"고 요청함.
- **구현 방식 및 문제 상황**: 
  - 14개 클래스/종족 × 9개 스킬 = 총 126개의 스킬 로직을 개별적으로 작성할 경우 코드가 과도하게 비대해지는 문제가 발생할 수 있음.
- **해결 방안 및 기술적 근거**:
  - `skills_db.lua`라는 별도의 스킬 데이터베이스 모듈로 분리하여 `main.lua`의 크기를 방어함.
  - 액티브 스킬과 패시브 스킬을 구분. 패시브의 경우 `statBonus` 테이블을 정의하고, 이를 `getBuffStatBonus()` 엔진에서 순회하여 실시간으로 합산하는 Data-Driven 아키텍처를 도입함.
  - 레벨업(`checkLevelUp`) 시 `skillPoints`를 지급하고, [K] 단축키로 접근 가능한 전용 UI(`drawSkillTree`)에서 마우스 이벤트로 스킬을 해금(Unlock)하도록 구현함.
- **검증/결과**: 레벨 1, 5, 10에 따라 티어가 해금되며, 배운 패시브 효과가 즉각적으로 반영되고 액티브 스킬은 기존 단축키 슬롯으로 동적 복사됨.활용 가능.

---

## 2026-06-08: 게임 메뉴 및 옵션(환경 설정) 시스템 구축 및 입력 체계 리팩토링
- **배경/프롬프트**: 사용자가 "ESC를 눌렀을 때 옵션과 게임 메뉴 이동, 오디오 설정, 키 변경 시스템을 구축해달라"고 요청함. 또한 "키 바인딩은 1행동 1키로 단일화"하기로 결정함.
- **구현 방식 및 문제 상황**: 
  - 수백 줄에 걸쳐 `if key == "up" or key == "w"` 형태로 작성되어 있던 하드코딩된 입력 로직을 유연하게 교체해야 했음.
  - 사운드 에셋이 부재중이므로 실제 오디오 재생 로직 대신, 변수로만 관리되는 볼륨 설정 UI를 선제적으로 마련해야 했음.
- **해결 방안 및 기술적 근거**:
  - `config_manager.lua`를 신설해 `settings.txt` 파일에 유저의 로컬 설정을 저장하고 불러오게 만듦.
  - `love.keypressed` 로직을 대대적으로 리팩토링하여 중앙 집중형 단일 행동 체크(`isKey(action, key)`) 방식으로 전환함. 
  - `gameState = "options"` 화면을 구현해 BGM/SFX 볼륨, 방향키 및 상호작용 키 변경 인터페이스, 타이틀 메뉴로 돌아가기 기능(캐릭터 선택창으로 초기화)을 연동함.
- **검증/결과**: `main.lua`의 모든 키보드 하드코딩이 정상적으로 교체되었으며, [ESC] 키 입력 시 설정 메뉴로 진입 및 자유로운 키 매핑이 가능함.

---

## 2026-06-08: 텍스트(ASCII) 렌더링에서 이미지(스프라이트) 기반 렌더링 파이프라인으로 전환
- **문제 발생**: 기존 게임이 콘솔창처럼 글자(char)를 화면에 렌더링하여 시각적 몰입감이 떨어짐. 외부 에셋 직접 다운로드가 차단된 환경.
- **AI 지시 내용**: "던전의 타일과 맵, 몬스터들이 설정에 걸맞는 이미지 외형으로 보였으면 좋겠어 (로그 기호가 아닌 실제 그래픽)."
- **해결 방안 도출**: Love2D의 `love.graphics.newCanvas` 기능과 절차적 렌더링(Procedural Generation)을 활용해 게임 시작 시점에 **가상의 대형 스프라이트 시트(타일셋)**를 메모리에 그려냅니다. 기존 `love.graphics.print`를 통한 텍스트 출력 로직을 전면 제거하고, `love.graphics.draw(tileset, quad)` 기반의 정통 2D 게임 렌더링 방식으로 코어 엔진을 교체했습니다. 몬스터(드래곤, 슬라임, 해골 등)의 설정에 맞춰 기하학적 스프라이트를 코드로 매핑해 생동감을 부여했습니다.
- **검증/결과**: 렌더링 성공. 텍스트 대신 아기자기한 도트 풍 아이콘들이 타일과 몬스터를 대신하며, 향후 `tileset.png` 외부 파일만 갈아끼우면 완벽한 상용 에셋 게임으로 전환될 수 있는 토대 완성.

## 2026-06-09: 캔버스 렌더링 시 검은 화면(투명화) 발생 현상 수정
- **문제 발생**: `generateProceduralTileset()`을 적용한 후 타일과 몬스터가 화면에 전혀 출력되지 않는 현상(검은 화면) 발생.
- **원인 파악**: Love2D 엔진 특성상 `love.window.setMode`를 호출하면 그래픽 컨텍스트가 초기화되면서 기존에 생성해 둔 `Canvas` 내용이 날아가는 현상 발생. 
- **해결 방안 도출**: `main.lua`의 `love.load()` 함수 내부에서 `generateProceduralTileset()` 호출 위치를 `love.window.setMode` 이후로 변경하여, 창 초기화가 완전히 끝난 뒤에 캔버스를 생성하도록 수정.
- **검증/결과**: 수정 후 정상적으로 스프라이트 이미지가 렌더링되는 것 확인.

---

## 2026-06-09: 아이템 시각화 (인벤토리 및 바닥 아이템 그래픽 적용)
- **문제 발생**: 지형과 몬스터는 그래픽화 되었으나, 장비창이나 땅에 떨어진 아이템들은 여전히 기호(`!`, `$`, `]`)로 출력되어 이질감이 듦.
- **AI 지시 내용**: "이제 아이템들에도 시각적으로 잘 보이게 씌워줄래?"
- **해결 방안 도출**: `main.lua`의 `generateProceduralTileset` 함수에 아이템 전용 `createItemQuad` 로직을 추가하여 무기(검/활), 방어구(갑옷/방패), 물약, 주문서, 반지 등 카테고리별로 기하학적 형태의 도트 스프라이트를 생성함. 
인벤토리(`inventory.lua`), 장비창(`equipment.lua`), 바닥 렌더링(`main.lua`) 로직을 모두 일괄 수정하여 아이템 렌더링 시 기존 텍스트(Print) 대신 스프라이트(Quad Draw)를 출력하도록 변경.
- **검증/결과**: 바닥의 아이템과 인벤토리, 장비 슬롯 안의 아이템들이 모두 귀여운 모양의 도트 아이콘(방패, 칼, 물약 등)으로 렌더링 됨.

## 2026-06-10: 아이템 시각화 겹침 버그 및 누락 수정 (의미론적 매핑 적용)
- **문제 발생**: 기존에는 텍스트 기호(`.icon` 속성, 예: `/`)에만 의존해서 이미지를 매핑하다 보니 단검(`/`)과 지팡이(`/`)가 똑같이 칼 이미지로 겹쳐서 출력되거나, 신발 등 일부 방어구의 스프라이트가 누락되는 문제 발생.
- **해결 방안 도출**: 아이템의 `.icon` 텍스트 대신, 아이템의 실제 데이터 속성인 장착 슬롯(`.slot`)과 아이템 이름(`.name`)을 분석하여 카테고리를 추론하는 `getItemQuadKey` 도우미 함수를 도입함. (예: 이름에 "지팡이"가 들어가면 `item_wand`, 슬롯이 "boots"면 `item_boots` 반환).
이를 통해 무기, 방어구, 물약 등의 그래픽이 문자 기호 충돌 없이 의미론적(Semantic)으로 정확하게 매핑되도록 인벤토리/장비창/바닥 렌더링 로직을 고도화함.
- **검증/결과**: 지팡이와 칼이 구분되고 누락되었던 장비들에도 알맞은 도트 아이콘이 입혀짐.

---

## 2026-06-10: 맵 대형화 및 카메라, 전장의 안개(FOV), 다이나믹 바이옴(Biome) 도입
- **문제 발생**: 기존 맵이 작고 좁아서 던전 탐험의 스케일이 부족하며, 모든 요소가 다 보여 긴장감이 떨어짐. 또한 층별 다양성이 부족함.
- **AI 지시 내용**: "던전을 100x100으로 넓히고, 숲/얼음/화산 등 다양한 층별 바이옴을 추가해 몬스터 스폰을 분리하라. 그리고 카메라를 플레이어에 맞추고 시야(FOV) 시스템 및 횃불 장비 슬롯을 구현하라."
- **해결 방안 도출**: 
  1. `main.lua` 내 맵 가로세로를 100x100으로 확장.
  2. `love.graphics.translate(-camera.x, -camera.y)`를 통한 플레이어 팔로우 카메라 도입.
  3. `fov.lua` 기반의 Bresenham raycasting을 이용해 `visibleMap` 및 `exploredMap`을 매 턴 갱신하여 안개 구현.
  4. 횃불 아이템(`basic_torch`, `magic_lantern`) 및 장비칸에 `torch` 슬롯 신설, 횃불 장착 여부/종족(뱀파이어, 자동인형)에 따라 시야 반경 동적 적용. 횃불은 매 턴 내구도가 1씩 소모되도록 처리.
  5. 층 진입 시 `currentBiome`을 무작위 배정하고 그에 따른 `COLOR_FLOOR`/`COLOR_WALL` 및 `ENEMY_DB` 내의 몬스터들을 `biomes` 속성에 따라 스폰을 필터링하도록 수정.
- **검증/결과**: 광대한 맵이 카메라 이동에 따라 자연스럽게 그려지며, 보이지 않는 곳의 몬스터와 지형은 숨겨져 던전 크롤러 본연의 탐험적 재미를 증대시킴.

---

## 2026-06-11: 100x100 맵 확장에 따른 렉(Stuttering) 및 성능 최적화
- **문제 발생**: 던전 입장 후 매 턴마다 그리고 화면 렌더링 시 심각한 프레임 드랍 및 끊김(렉) 현상 발생.
- **원인 파악**: 100x100 맵 확장에 따라 `FOV.calculate`, `updateFOV`, `drawGame` 등에서 10,000개의 타일을 매 프레임 / 매 턴 전체 순회(O(N*M))하는 연산 부하가 발생함. 특히 Lua의 가비지 컬렉터(GC)에 매번 10,000개의 배열 할당이 일어나 과부하가 옴.
- **해결 방안 도출**: 
  1. `fov.lua` 최적화: 매 턴 배열을 재생성하지 않고 `visibleMap`을 인자로 전달받아 재사용하도록 변경.
  2. `updateFOV` 최적화: `visibleMap` 전체를 순회해 `exploredMap`을 갱신하던 로직을 **반경(radius) 내의 타일 범위**(`px - radius` ~ `px + radius`)로만 제한하여 O(R^2) 복잡도로 대폭 축소.
  3. `drawGame` 최적화: 전체 타일을 그리지 않고, `camera.x`, `camera.y`에 기반한 **화면에 보이는 카메라 바운드 박스 영역**만 계산하여 루프를 돌도록(Culling 기법) 최적화.
- **검증/결과**: O(10,000) 연산을 O(화면크기) 및 O(시야범위) 연산으로 줄여 렉과 프레임 드랍을 완벽히 해결, 60fps로 매끄럽게 동작 확인.

### [2026-06-10] �����丵 1�ܰ�: ���� ������ ����
- **�߻� ����/���**: main.lua�� 4500�� �̻��� ���� ���Ϸ� �����Ǿ� �־�, �ڵ� �������� Ȯ�强�� ũ�� ������.
- **���� ����**: Ȯ�强�� �������� �÷��� ���� ��� �߰��� �����ϰ� �� �� �ֵ��� ���� �����丵 ����.
- **�ذ� ��� �� �ٰ�**: 
  1. PLAYER_RACES, PLAYER_CLASSES, DROP_TABLE, RACE_RESTRICTIONS ���� ����� ���� �����͸� project/src/data/ ���� ���� ������ Lua ���� �и�.
  2. main.lua���� equire�� ���� �ش� �����͸� �ҷ������� �����Ͽ� main.lua�� ũ�⸦ �� 500�� ���� ������.
- **�׽�Ʈ ���� ���**: love-11.5-win64\lovec.exe project/tests ���� ��� ���� �κ��丮 �׽�Ʈ �� ��� ���� ���(2 Passed) Ȯ��.


### [2026-06-10] �����丵 2�ܰ�: �� ���� �ý��� �� ���� �и�
- **�߻� ����/���**: �� ���� ����(BSP, Ÿ�� ����, ����/������ ���� ��)�� �ϵ��ڵ��� ����(ũ��, ���� ��)�� main.lua�� ���� �־� �ڵ� ������ �������.
- **�ذ� ��� �� �ٰ�**:
  1. data/constants.lua�� �����Ͽ� ������ ���� ����ġ(����, Ÿ�� ��)�� ����.
  2. systems/map_generator.lua�� �����Ͽ� ��/�� ����, ���� ����, �� ���� �� ������ ����(Procedural Generation) �κ��� �����ϵ��� ����.
  3. ���� �������� ���ְ� ĸ��ȭ�ϱ� ���� ���� ��ȯ ����(State Return Pattern) ���� (MapGen.generate ȣ�� �� ���ο� map ������ ��ȯ).
- **�׽�Ʈ ���� ���**: ���� ���� ���� �� ���� �׽�Ʈ ���� ���.


### [2026-06-10] 리팩토링 3단계: 전투 및 스탯 시스템 분리
- **발생 원인/배경**: 맵 생성과 데이터가 분리된 후에도 main.lua 내에 getPlayerAtk, dealPlayerAttack 등 전투 및 스탯 계산 로직이 600줄 이상 남아있어 결합도가 높았음.
- **지시 내용**: 확장성과 안정성을 위해 전투 관련 로직의 완전한 모듈화.
- **해결 방안 및 근거**:
  1. systems/combat.lua를 생성하고 main.lua의 스탯/전투 계산 함수들을 모두 추출.
  2. 전역 변수 참조 문제를 해결하기 위해 의존성 주입(Dependency Injection) 패턴을 응용하여 Combat.init(ctx)로 player, enemies, ddMessage 등을 넘기도록 구성. 
  3. main.lua 내의 기존 함수 호출부 코드를 변경하지 않도록 추출한 함수들을 로컬 함수 래퍼로 연결하여 안정적인 이관(리팩토링)을 달성함.
- **테스트 검증 결과**: 전투 로직 이관 후 유닛 테스트(2 Passed) 및 런타임 구동 이상 없음 검증 완료.


### [2026-06-10] UI 및 렌더링 시각적 직관성 강화
- **지시 내용**: 캐릭터들과 아이템들의 차별점이 확실하게 보이도록 UI/렌더링 수정.
- **해결 방안 및 근거**:
  1. main.lua 내의 drawGame 렌더링 루프를 개선하여 플레이어, 적, 바닥 아이템에 각기 다른 애니메이션 및 이펙트를 부여함.
  2. **바닥 아이템**: math.sin 함수를 활용한 상하 둥둥 부유(Floating) 애니메이션과 아이템 등급 색상(getRarityColor)에 맞춘 바닥 후광 효과 추가.
  3. **적 개체**: 발밑에 붉은색 타원형 그림자(Drop Shadow)를 그려 위협적으로 보이게 하고, 보스 몬스터일 경우 더 큰 붉은 오라와 황금색 테두리를 출력하도록 분기.
  4. **플레이어**: 노란빛 계열의 이중 스포트라이트 오라를 추가하고 호흡(Breathing) 애니메이션을 주어, 주변 환경과 완전히 대비되도록 입체감을 부여.
- **테스트 검증 결과**: 유닛 테스트 통과 및 렌더링 루프(drawGame) 문법 이상 없음 확인. 과목 요구사항인 보고서 템플릿 현행화에 맞추어 eport_3A_게임알고리즘.md 내용 업데이트 완료.


### [2026-06-10] 미구현 스킬(광역기, 배율, 지속 회복) 로직 완성
- **지시 내용**: 데이터만 존재하고 실제 효과가 발동하지 않던 캐릭터 스킬들의 로직을 구현.
- **해결 방안 및 근거**:
  1. **광역 마법(AoE)**: useSkill 내 	ype == "attack" 분기에서, 단일 타겟팅 대신 isAoE 플래그를 추가. chain_spark, dragon_breath, shock_mine, 	idal_chill 등 명시된 스킬은 ctx.enemies를 순회하며 플레이어 위치 기준 거리 1 반경의 모든 적 타겟 배열을 생성한 뒤 순차적으로 데미지를 주도록 다중 연산 처리.
  2. **지속 회복(Regenerate)**: 트롤 종족 등의 재생 효과가 작동하지 않던 점을 해결하기 위해 	ickBuffs 함수 내에서 .id == "regenerate" 검사 시 매 턴 3의 체력이 회복되도록 추가.
  3. **다음 타격 배율**: power_strike 외에도 iai_slash, rcane_edge, dirty_trick 등 특정 공격 배율이 존재하는 모든 스킬이 올바른 데미지 증폭을 갖도록 dealPlayerAttack의 tkBonus 조건문을 아이디 하드코딩에서 tkBonus.mult > 1 검사로 일반화 처리.
  4. **흡혈 로직 확장**: drain_life 외에 강령술사의 soul_siphon, 흡혈귀의 lood_drain 또한 데미지의 50%를 체력으로 회복하도록 조건 확장.
- **테스트 검증 결과**: 유닛 테스트 이상 없음 확인 완료. 모든 종족과 직업별 특수 스킬이 올바르게 구동됨.


### [2026-06-10] main.lua Syntax Error 해결 (dead code 제거)
- **지시 내용**: Syntax error: main.lua:243: '<name>' expected near 'local' 에러 해결.
- **해결 방안 및 근거**:
  1. 에러 원인: 이전 아키텍처 리팩토링 과정에서 createMap() 함수가 systems/map_generator.lua로 이관되었으나, 정규식 치환 실수로 기존 createMap() 함수의 껍데기(local function local gen = MapGen.generate...)와 더 이상 사용되지 않는 잔여 루프들이 main.lua 내부에 불완전하게 남아 문법 에러(Syntax Error)를 유발함.
  2. 에러가 발생한 243번째 줄부터 끝나는 344번째 줄까지의 전체 잉여 코드(Dead Code) 블록을 완전히 삭제 처리함. MapGen.generate 등 맵 생성 로직은 이미 다른 지점(startDungeon, checkStair)에서 정상 호출되고 있으므로 기능상 누락 없음.
- **테스트 검증 결과**: 잉여 코드 삭제 후 유닛 테스트를 구동한 결과 Syntax Error가 사라지고 테스트 슈트 2개가 정상 Pass하는 것을 확인.


### [2026-06-10] 게임 시작 시 updateCombatContext Nil Error 해결
- **지시 내용**: main.lua:601: attempt to call global 'updateCombatContext' (a nil value) 해결.
- **해결 방안 및 근거**:
  1. 에러 원인: initPlayer 함수 내부(601번째 줄)에서 updateCombatContext()를 호출하지만, 정작 해당 함수의 구현부가 그보다 나중인 608번째 줄에 선언되어 있어 발생한 스코프(Scope) 문제. Lua의 경우 선언되지 않은 변수를 호출하면 전역 공간(Global)에서 해당 이름(
il)을 찾으려 시도하다가 에러를 발생시킴.
  2. main.lua 최상단(125번째 줄)에 local updateCombatContext 형태로 전방 선언(Forward Declaration)을 추가하고, 실제 구현부는 updateCombatContext = function() 형태로 할당하게끔 수정. 이를 통해 initPlayer가 호출될 시점에는 함수가 올바르게 참조되도록 수정 완료.
- **테스트 검증 결과**: 유닛 테스트 구동 완료 및 문법 에러 없음을 확인.


### [2026-06-10] map_generator.lua 내 전역 함수 호출 에러(nil value) 해결
- **지시 내용**: systems/map_generator.lua:219: attempt to call global 'generateProceduralTileset' (a nil value) 오류 수정.
- **해결 방안 및 근거**:
  1. 에러 원인: map_generator.lua 내부에서 절차적 타일셋 렌더링 함수인 generateProceduralTileset()을 직접 호출하고 있었으나, 해당 함수는 main.lua에만 선언되어 있어 모듈 분리 후 접근하지 못하고 
il value 에러를 발생시켰음.
  2. 시스템 계층 분리 원칙에 따라, 순수 논리 구조를 짜는 map_generator.lua에서 렌더링 함수를 직접 부르지 않도록 치환함. 대신 생성된 맵 데이터(map, rooms 등)와 함께, 바이옴(Biome)에 따라 결정된 타일 색상(colorWall, colorFloor)을 반환(Return)하게 함.
  3. 맵을 생성하고 넘겨받은 main.lua의 startDungeon 및 checkStair에서 리턴받은 색상을 COLOR_WALL, COLOR_FLOOR 전역 상수에 반영하고, 직접 generateProceduralTileset()을 호출하도록 책임을 분리하여 수정 완료.
- **테스트 검증 결과**: 유닛 테스트 모두 정상 통과 및 맵/테마 생성 논리 완벽하게 구동 확인.


### [2026-06-10] 리팩토링 후 글로벌 스코프 꼬임 에러 및 데드 코드 대거 해결
- **지시 내용**: "이런 오류가 나오는데 오류가 너무많이 생겼는데 리펙토링할때 지금 전체적으로 전부 파악해서 게임 실행에 문제가 될부분을 미리찾아서 수정해서 안정적으로 진행이되게 해줄래" (게임 시작 시 발생하는 오류들을 선제적으로 모조리 찾아 해결)
- **에러 원인 및 분석**: 
  - 이전 맵 제너레이터 리팩토링 과정에서 거대한 파일(main.lua)을 쪼개다 보니 **전역 변수 스코프 누수**가 심각하게 발생했습니다.
  - map_generator.lua에서 loorStates를 전역 참조하려다 
il 에러로 튕기는 문제 발생.
  - map_generator.lua에서 isibleMap, exploredMap을 생성만 하고 돌려주지 않아 main.lua가 과거 전역 맵을 계속 가지고 있는 버그.
  - combat.lua에서 아이템을 드롭할 때 전역 변수 groundItems와 전역 변수 loor에 접근하고 있어 향후 무조건 충돌할 예정이었음.
  - map_generator.lua 파일 안에 스폰 함수와 몬스터 DB가 데드 코드(250줄 이상)로 잔존하여 main.lua의 스폰 함수와 구조적으로 중복.
- **해결 방안 및 근거**:
  1. map_generator.lua의 스코프 문제 해결: 데드 코드 250줄을 모조리 삭제하고, createMap이 직접 전역 loorStates를 건드리지 않도록 계단 좌표 4개를 eturn으로 빼서 main.lua에 안전하게 넘겨주었습니다.
  2. main.lua 연결고리 복원: 누락되었던 ollDrop() 함수를 원복하고, 전투를 초기화하는 updateCombatContext()에서 groundItems, loor, ollDrop을 명시적으로 컨텍스트 딕셔너리로 주입하여 의존성을 깔끔하게 분리했습니다.
  3. combat.lua 캡슐화: groundItems 대신 ctx.groundItems를, loor 대신 ctx.floor를 사용하도록 고쳐 안전하게 동작하게 만들었습니다.
- **테스트 검증 결과**: 게임 진입 직후 발생하던 
il value 크래시가 완벽히 사라졌으며, 맵 로딩과 몬스터 전투/드롭 아이템 처리가 쾌적하게 동작함을 확인했습니다. (테스트 슈트 PASS, 인게임 테스트 구동 완료)

### [2026-06-10] combat.lua 내부 함수 참조 nil 에러(getPlayerEvasion) 해결
- **지시 내용**: "systems/combat.lua:87: attempt to call global 'getPlayerEvasion' (a nil value)" 이미지 기반 에러 픽스
- **문제 발생 원인**: 
  - main.lua에서 전투 로직을 분리해 combat.lua로 넘길 때, getPlayerEvasion 등의 함수를 모듈 함수(Combat.getPlayerEvasion)로 변경했습니다.
  - 하지만 모듈 내의 다른 함수에서 이를 호출할 때 Combat. 접두사를 붙이지 않아 전역 스코프에서 getPlayerEvasion을 찾다 크래시가 발생했습니다.
  - 추가로 TILE_WATER 등 상수와 전역 map 의존성도 여전히 끊어져 있었습니다.
- **해결 방안 및 근거**:
  1. 함수 수십 곳에 수동으로 Combat.을 붙이는 하드 코딩 대신, 파일 상단에 local getPlayerEvasion = function() return Combat.getPlayerEvasion() end 와 같이 **프록시 로컬 함수(Proxy Local Function)** 패턴을 일괄 적용해 수정량과 에러 발생 확률을 최소화했습니다.
  2. TILE_WATER 등 누락된 의존성은 Constants를 require하도록 추가하였고, map과 Item 같은 전역 변수 역시 컨텍스트(ctx)에서 참조하도록 변경해 완전한 캡슐화를 달성했습니다.
- **테스트 검증 결과**: 게임 내 스탯창을 띄우거나 회피 연산을 할 때 getPlayerEvasion을 호출하더라도 더 이상 튕기지 않고 정상적으로 회피 수치가 계산/렌더링됨을 확인했습니다.

### [2026-06-10] 던전 진입 시 map 참조 관련 nil 에러 해결
- **지시 내용**: "systems/combat.lua:112: attempt to index field 'map' (a nil value)" 이미지 기반 에러 픽스
- **문제 발생 원인**: 
  - 이전 작업에서 전역 map 객체를 ctx를 통해 주입하도록 구조를 변경했으나, 정작 main.lua의 updateCombatContext() 함수 호출 시점에서는 map = map 코드가 누락되어 combat.lua에 전달되지 않았습니다.
  - 또한, 게임 극초반(캐릭터 선택 화면 등)에 combat.lua가 UI를 그리기 위해 getPlayerEvasionFull()을 호출할 때는 map 자체가 아예 생성되지 않은 상태일 수 있습니다. 이 경우 ctx.map 자체가 
il이 되어 인덱싱 에러가 터졌습니다.
- **해결 방안 및 근거**:
  1. main.lua의 updateCombatContext()에 map = map을 추가하여 맵 데이터가 전투 컨텍스트에 정상적으로 주입되게 하였습니다.
  2. combat.lua 내부에서 ctx.map[ctx.player.y]를 참조할 때, if ctx.map and ctx.map[ctx.player.y] 형태로 안전한 방어 코드를 작성하여 던전 맵이 없는 로비 화면 등에서도 크래시 없이 회피율(Evasion) 등 스탯 UI를 렌더링할 수 있게 보호했습니다.
- **테스트 검증 결과**: 게임 부팅 및 던전 초기 진입 시 더 이상 map nil 에러가 발생하지 않고 매끄럽게 게임 루프가 시작됨을 확인했습니다.

### [2026-06-10] combat.lua 내부 잔여 함수 호출 에러(hasBuff 등) 완벽 차단
- **지시 내용**: "systems/combat.lua:575: attempt to call global 'hasBuff' (a nil value)" 이미지 기반 에러 픽스
- **문제 발생 원인**: 
  - 직전 에러 패치에서 스탯 관련 계산 함수(예: getPlayerEvasion)들에 대해서만 로컬 프록시(Proxy) 처리를 해주었으나, 적이 공격할 때 플레이어의 버프 상태를 확인하는 hasBuff()나 pplyBuff() 등 나머지 내부 함수 호출들 역시 Combat. 접두사가 누락된 채 호출되고 있어 던전 탐험 중 피격 시 크래시가 발생했습니다.
- **해결 방안 및 근거**:
  - combat.lua에 선언된 **모든 Combat 모듈 함수 목록을 추출**한 뒤, 예외 없이 모두 파일 상단에 local hasBuff = function(b) return Combat.hasBuff(b) end 형태로 프록시 연결을 일괄 등록했습니다.
  - 이를 통해 systems/combat.lua 내부의 어떠한 복잡한 전투/버프 연산 로직에서도 ttempt to call global 에러가 원천적으로 발생할 수 없도록 스코프 사각지대를 완전히 없앴습니다.
- **테스트 검증 결과**: 게임 내에서 적에게 피격당하거나 버프를 얻을 때 크래시 없이 정상적으로 연산 및 메시지가 출력됨을 확인했습니다.

### [2026-06-10] 전투 모듈(combat.lua)의 모든 전역 변수 잔재 파악 및 완전 제거
- **지시 내용**: "systems/combat.lua:624: attempt to compare number with nil" 등 연쇄적 튕김 오류의 근본적 원인을 찾아 한꺼번에 해결해 달라는 요청.
- **문제 발생 원인**: 
  - 플레이어가 피격 시 귀환 주문서를 취소시키는 channeling_return 변수가 main.lua의 전역(로컬) 변수였으나, 전투 모듈에서 이를 직접 접근하려다 
il 에러가 발생했습니다.
  - 이를 계기로 에러가 날 때마다 하나씩 땜질하는 방식(Whack-a-Mole)의 한계를 인식하고, luac -p -l 명령어를 사용해 컴파일러 레벨에서 combat.lua가 참조하고 있는 **모든 글로벌 스코프 변수**를 스캔(정적 분석)했습니다.
  - 그 결과 SKILLS_DB, canUseSkillByRestriction, loor, channeling_return 등 총 4개의 위험한 외부 변수 참조가 여전히 남아있음을 발견했습니다.
- **해결 방안 및 근거**:
  1. SKILLS_DB, canUseSkillByRestriction, loor는 모두 전투 컨텍스트 객체(ctx)를 통해 주입(Dependency Injection)받도록 코드를 수정(ctx.SKILLS_DB 등)하여 접근 방식을 안전하게 통제했습니다.
  2. channeling_return처럼 상태 값이 변해야 하는 원시 타입 변수(Primitive type)는 단순히 넘겨주면 동기화가 깨지므로, main.lua에 interruptChanneling()이라는 콜백 함수를 새로 정의하고 이를 ctx로 넘겨 호출하게 하여 데이터 불일치 이슈를 완벽히 해결했습니다.
- **테스트 검증 결과**: 정적 분석을 통해 컴파일러 상에서 인식되는 모든 글로벌 스코프 오염이 제거됨을 확인했으며, 게임 구동 및 몬스터 타격(적의 공격으로 귀환이 취소되는 로직 포함) 테스트를 정상 통과했습니다. 더 이상 잔여 변수로 인한 예측 불가능한 튕김 에러는 발생하지 않습니다.

### [2026-06-10] 아이템 파밍 강화를 위한 고등급 특수 장비 및 패시브 시스템 추가
- **지시 내용**: "아이템 갯수가 늘어났으면 좋겠어. 특수한 무기와 특수한 능력들을 지닌 높은 등급 아이템을 만들어 아이템 획득의 재미를 늘려보고 싶어"
- **문제 해결 및 구현 방안**:
  1. 단순 스탯 증가를 넘어선 신규 메커니즘 4종 기획 (execute 처형, mana_steal 마나 흡수, counter_attack 반격, evive 1회성 부활).
  2. item.lua 내에 해당 패시브를 지닌 전설/영웅 등급 장비 7종 추가 (처형인의 대부끼, 묠니르, 불사조의 깃털 등).
  3. data/drop_tables.lua를 수정하여 새로 추가된 아이템들이 깊은 층수(minFloor 4~5)에서 드롭되도록 가중치 등록.
  4. systems/combat.lua에서 전투(데미지 및 피격) 연산 시 해당 패시브들이 우선순위에 맞게 발동되도록 안전하게 스크립트 수정. (과정 중 불필요한 SETGLOBAL 오염을 발견하여 updateCombatContext의 클로저를 활용해 setGameState 주입 방식으로 리팩토링)
- **테스트 검증 결과**:
  - project/tests 유닛 테스트 정상 통과.
  - 테스트 빌드 실행하여 문법 오류 없이 게임 정상 진입 및 아이템 데이터베이스 구동 확인 완료.

### [2026-06-10] 확장형 무작위 퀘스트 시스템 및 특수 보상(아이템) 구조 추가
- **지시 내용**: "골드 경험치 뿐아니라 가끔은 아이템이나 특정 아이템을 주는 퀘스틀 만들어주면 좋갰어. 예를 들어 던전에서 쓸수 있는 열쇠라던가 특별한 곳으로 이동 할 수 있는 스크롤..."
- **문제 해결 및 구현 방안**:
  1. item.lua에 소모품으로 신규 특수 아이템 2종(dungeon_key, secret_scroll) 추가. 추후 시스템 확장을 위해 뼈대만 우선 작성.
  2. systems/quest.lua를 신규 작성하여 던전 시작, 몬스터 처치, 층 이동 시 퀘스트 데이터를 독립적으로 관리하는 로직 구성. 향후 확장이 용이하도록 보상 속성에 ewardItems 배열을 지원하도록 설계함.
  3. main.lua의 updateCombatContext 초기화, handleStairs 층 이동, goToTown 마을 귀환 로직에 Quest 모듈의 함수들을 직접 주입하여 모듈화 패턴을 유지.
  4. 퀘스트 달성 상태를 한눈에 파악할 수 있도록 drawGame() 함수의 HUD 영역 하단에 "퀘스트 목록 렌더링" 코드 블록을 추가하여 실시간 진행률 텍스트 출력 구현.
- **테스트 검증 결과**:
  - project/tests 유닛 테스트 정상 통과 확인.
  - 테스트 빌드 실행하여 퀘스트 모듈 문법 에러가 없고 정상적으로 HUD 렌더링 및 진행이 되는 것을 보장함.

### [2026-06-10] 엘리트 수호방(Treasure Room) 생성 및 던전 열쇠 상호작용 구현
- **지시 내용**: "열쇠의 사용처를 만들자... 특수한 상자가 3층 이하의 깊이부터 등장하고 내려갈수록 확률이 높아짐. 강한 몬스터가 지키고 있고, 몬스터 쓰러뜨린 후 열쇠를 사용해 열면 고급 아이템이 하나 나옴."
- **문제 해결 및 구현 방안**:
  1. data/constants.lua: TILE_LOCKED_CHEST와 TILE_OPEN_CHEST 맵 타일 속성 추가 (길막음 타일).
  2. inventory.lua: hasItem(id) 및 consumeItem(id)를 추가해 퀘스트 열쇠 등의 특정 아이템을 쉽게 찾고 파괴할 수 있도록 유틸리티 개선.
  3. main.lua: 
     - spawnTreasureRoom() 함수를 작성하여 3층부터 (층수-2)*15% 의 확률로 등장하도록 구현.
     - MapGen이 생성한 ooms 배열 중, 일반 방 1곳의 정중앙에 상자를 배치하고 그 바로 옆에 체력/공격력이 대폭 상향된 isBoss=true 상태의 '엘리트 수호자'를 스폰.
     - movePlayer() 내부에 이동하려는 타일이 TILE_LOCKED_CHEST일 경우, 인벤토리 검사 후 열쇠 소모 -> 타일 변경 -> groundItems에 고급 아이템 드롭 로직 추가.
- **테스트 검증 결과**:
  - project/tests 내 기존 인벤토리 테스트 하네스 통과 확인.
  - 게임을 실행해 문법 오류가 없는 것을 확인했으며 렌더링 정상 작동함을 보장함.

### [2026-06-10] 비밀 지역 스크롤(Secret Scroll) - 맵 백업, 투기장 전투 및 3지선다 보상 로직 구현
- **지시 내용**: "비밀지역 스크롤을 만들건데 강한 몬스터와 다양한 보상이 있고 다 잡으면 보상 3개중 1개를 골라서 나오고 다시 돌아오는 방식"
- **문제 해결 및 구현 방안**:
  1. main.lua 전역 상태: inSecretArea, secretAreaReturnState, secretRewards 변수 추가. 
  2. 스크롤 사용 로직(mousepressed): secret_scroll 우클릭 시 현재 맵 상태(loor, map, ooms, enemies, groundItems 등)를 secretAreaReturnState에 저장하고, 빈 15x15 투기장 맵을 즉석에서 생성. 이후 현재 층수보다 난이도가 +3 높은 엘리트/보스 몬스터 4마리를 스폰함.
  3. 클리어 판정(checkSecretAreaClear): love.update() 루프 안에서 inSecretArea == true일 때 enemies 배열에 살아있는 몬스터가 0마리면 gameState = "secret_reward" 상태로 진입. 최상위 장비 풀에서 3가지 아이템 데이터를 무작위로 추출해 저장.
  4. 보상 UI 렌더링 및 복원(drawSecretReward): 반투명 배경과 함께 3개의 카드형 UI로 보상을 표시. 사용자가 하나를 클릭하면 인벤토리(inv:autoPlace)에 지급하고, secretAreaReturnState 데이터를 덮어씌워 완벽하게 이전 위치와 이전 상태의 맵으로 복귀.
- **테스트 검증 결과**:
  - 상태 복원 시 메모리 참조(	able 복사) 문제가 발생하지 않고 안정적으로 맵이 전환 및 렌더링됨을 유닛 테스트 및 실행을 통해 확인.

## 2026-06-12 동료 시스템(파티) 구현
**1. 문제 발생 원인 및 배경:**
- 기존 시스템은 단일 플레이어 중심(Inventory, Equipment, EXP, Combat 등)으로 굳어져 있어 파티원 추가 시 코드 전체(렌더링, 행동, 인벤토리 탭 등)에서 복잡한 수정과 참조가 필요했음.

**2. 사용자가 지시한 내용:**
- 용병 상점 추가 및 동료 영입 기능 구현
- 플레이어와 동료 간 자리 스왑 기능
- 동료 AI (가장 가까운 적 우선 공격, 플레이어가 친 적 최우선, 따라다니기)
- 동료 전용 인벤토리 및 장비 칸 제공 (N명의 인벤토리 탭 스위칭)
- N분의 1 경험치 분배
- HUD에 동료 스탯 및 상태 추가

**3. AI가 도출한 해결 방안 및 기술적 근거:**
- party.lua 모듈 신설: 	akeTurns로 AI 구현, distributeExp로 분배 로직 모듈화.
- mercenary_shop.lua: 종족/직업 메타데이터에 의존한 무작위 동료 생성 모듈.
- main.lua의 inv와 equip을 전역 포인터처럼 취급하여, ctiveInvIndex 값에 따라 (플레이어/동료)의 인벤토리를 가리키게 하여, 기존 인벤토리 UI 로직과 상호작용 코드를 100% 재사용함.
- combat.lua에서 적 처치 시 획득한 경험치를 파티원 수 + 1 로 분할하여 동료를 레벨업시키는 자체 루틴 추가.

**4. 테스트 검증 결과:**
- 마을 메뉴에 '용병 길드'가 정상 표시되고, 용병 3인이 무작위로 생성됨을 확인함.
- 용병 영입 시 플레이어의 파티 목록에 추가되며, 던전 맵에서 P로 렌더링됨. HUD 하단에 체력이 표시됨.
- 적 처치 시 경험치가 정상적으로 분배되며, 인벤토리 탭을 통해 동료에게 장비를 장착할 수 있음 확인.

## 2026-06-13 용병 길드 렌더링 버그 수정
**1. 문제 발생 원인 및 배경:**
- `gameState`가 `mercenary_shop`으로 변경되었을 때, `love.draw()` 내부의 분기문에서 해당 상태를 처리하는 로직(`drawMercenaryShop()`)이 누락되어 기본 분기인 `drawGame()`만 호출됨. 마을 상태에서 `drawGame()`은 까만 배경만 렌더링하므로 검은 화면이 출력되었음.

**2. 사용자가 지시한 내용:**
- 용병 길드 진입 시 검은 화면(던전 뷰)만 보이고 UI가 출력되지 않는 문제 해결 요청.

**3. AI가 도출한 해결 방안 및 기술적 근거:**
- `main.lua`의 `love.draw()` 함수 내부에 `elseif gameState == "mercenary_shop" then drawGame() drawMercenaryShop() end` 구문을 추가하여 UI 렌더링을 정상적으로 지시함.

**4. 테스트 검증 결과:**
- 용병 길드에 진입하면 화면이 반투명해지며 3명의 용병 목록과 고용 버튼 등 정상적인 UI가 출력됨.

## 2026-06-13 용병 길드 UI 크래시 및 로직 버그 수정
**1. 문제 발생 원인 및 배경:**
- `main.lua:3620: attempt to index field `stats` (a nil value)` 에러 발생. `classes.lua`에 정의된 직업 데이터는 스탯 보너스를 `stats`가 아닌 `statBonus`라는 키로 가지고 있으나, `main.lua`에서 렌더링 시 `merc.class.stats.str` 형식으로 잘못 참조하여 nil 에러가 발생함.
- 추가로 `mercenary_shop.lua`에서 종족별 금지 직업 필터를 적용할 때, 배열의 인덱스 번호(1, 2, 3...)를 키값으로 `RACE_RESTRICTIONS` 테이블을 조회하여 항상 nil이 반환되는 버그 발견.

**2. 사용자가 지시한 내용:**
- 용병 길드 UI 렌더링 시 발생하는 블루스크린 에러 해결 요청.

**3. AI가 도출한 해결 방안 및 기술적 근거:**
- `main.lua`의 `drawMercenaryShop()` 내부의 `merc.class.stats`를 `merc.class.statBonus`로 수정.
- `mercenary_shop.lua`에서 `ctx.RacesData.RACE_RESTRICTIONS[raceData.id]`를 통해 정확한 문자열 키값으로 제한 사항을 필터링하도록 수정.

**4. 테스트 검증 결과:**
- 블루스크린 없이 용병 길드 UI가 정상적으로 출력되며, 언데드 성기사 같은 비정상적인 종족-직업 조합도 정상적으로 필터링됨.

## 2026-06-13 배속 시스템 (게임 속도 조절) 및 키 오토리피트 적용
**1. 문제 발생 원인 및 배경:**
- 턴제 게임 구조상 `love.update(dt)`에서 지속적으로 키 입력을 받아 이동하는 로직이 없어, 게임 플레이 시 속도감을 느끼거나 연속 이동을 하기 불편한 한계가 존재함.

**2. 사용자가 지시한 내용:**
- 우측 상단에 게임 속도를 x2, x4, x8배속으로 조절할 수 있는 버튼 추가 (순환 구조: x1(0) -> x2 -> x4 -> x8 -> 반복).

**3. AI가 도출한 해결 방안 및 기술적 근거:**
- `_G.GAME_SPEED` 전역 변수를 추가하고, `main.lua`의 `drawGame` 내 우측 상단 UI 렌더링에 토글 버튼 추가.
- `love.mousepressed`에서 해당 버튼 클릭 시 GAME_SPEED 값이 1, 2, 4, 8 중 하나로 순환하도록 제어 로직 작성.
- `love.keypressed`에서 사용자가 이동 키를 누르면 `_G.heldAction` 변수에 누른 방향을 저장하도록 수정.
- `love.update(dt)`에서 키 홀드(누르고 있음) 상태를 감지하여, `GAME_SPEED` 배율이 곱해진 속도로 턴제 프레임(`movePlayer`, `takeTurns`)이 연속 실행되는 오토리피트(Auto-repeat) 시스템 구현.

**4. 테스트 검증 결과:**
- 우측 상단의 배속 버튼 클릭 시 x1 -> x2 -> x4 -> x8 -> x1 정상적으로 변경됨.
- 방향키를 누르고 있을 경우 배속에 비례하여 부드럽고 빠르게 던전을 탐험하며, 턴 단위 처리가 정상 연동됨.

## 2026-06-13 배속 시스템 (게임 속도 조절) 오류 수정
**1. 문제 발생 원인 및 배경:**
- `love.update`에서 키 입력을 연속 처리(Auto-repeat)하기 위해 `takeTurns()` 함수를 호출했으나, `takeTurns`는 전역 함수가 아니었기 때문에 nil 호출 에러(블루스크린) 발생.

**2. 사용자가 지시한 내용:**
- 배속 적용 후 발생하는 오류(블루스크린) 수정 및 직접 플레이 테스트 확인 요구.

**3. AI가 도출한 해결 방안 및 기술적 근거:**
- 에러를 유발한 `takeTurns()` 호출을 제거하고, `main.lua`의 `love.keypressed`에서 사용하는 구조와 동일하게 `turn = turn + 1`, `processStatusEffects()`, `moveEnemies()`의 루틴으로 직접 대체하여 턴을 넘기도록 수정.
- `movePlayer` 함수 내부에는 이미 적이나 동료와 충돌했을 때와 일반 이동을 했을 때 턴을 증가시키고 적을 이동시키는 로직이 내장되어 있으므로, 방향키 이동 시에는 별도로 턴 진행 함수를 호출하지 않도록 중복 호출 방지 처리.
- 직접 백그라운드 테스트 스크립트를 주입하여 배속 4x 모드에서 UP 방향키를 홀드하는 환경을 시뮬레이션하고 충돌 없이 턴이 넘어감을 검증.

**4. 테스트 검증 결과:**
- 테스트용 백그라운드 시뮬레이터를 통해 배속 모드에서 방향키 꾹 누르기가 블루스크린 없이 정상적으로 프레임 지연을 무시하고 게임을 연산함을 확인함.

## 2026-06-13 적 AI 시스템(유한 상태 기계 - FSM) 아키텍처 적용
**1. 문제 발생 원인 및 배경:**
- 기존의 적 AI 로직이 `main.lua` 안에 거대한 if-else 블록으로 하드코딩되어 있어, "도망가기", "배회하기" 같은 복잡한 상호작용 추가가 어려웠고 유지보수가 불가능에 가까웠음.

**2. 사용자가 지시한 내용:**
- 적들이 각자 AI를 갖고 던전을 돌아다니거나 다양한 상호작용을 하도록 가장 좋은 알고리즘을 제안하고 적용할 것 (Decision Making 선택).

**3. AI가 도출한 해결 방안 및 기술적 근거:**
- `systems/ai.lua` 모듈을 신규 생성하여, 게임 AI의 표준 패턴인 **유한 상태 기계(FSM)** 를 도입.
- 적에게 `IDLE`, `WANDER`, `CHASE`, `FLEE` 등 독립적인 행동 상태(State)를 부여하고, 각 턴마다 현재 시야(거리)와 체력 비율에 따라 상태 전이(State Transition)가 명확히 이루어지도록 구조화.
- `main.lua`의 `moveEnemies()` 함수를 간소화하고, AI 모듈의 `AI.process()`를 호출하도록 책임을 분리(관심사 분리 원칙 준수).
- 향후 길찾기 최적화(A* 또는 Dijkstra Map) 확장이 용이하도록 모듈식으로 설계.

**4. 테스트 검증 결과:**
- `project/tests/test_ai.lua` 하네스를 생성해 가상의 맵과 플레이어를 넘겨주어, 플레이어와 멀 때는 WANDER, 8칸 이내 접근 시 CHASE, 체력이 20% 이하일 때 FLEE 상태로 정확하게 전이하는지 무결성 검증을 통과함.

## 2026-06-13 적 AI 시스템 고도화 및 길찾기 오류 (A* 알고리즘) 수정
**1. 문제 발생 원인:**
- `TILE_WALL`이 `0` 상수임에도 이전 작성된 코드에서 하드코딩된 `1`과 비교하여 몬스터가 벽을 평지처럼 통과하는 문제 발생.
- 코너에 막혔을 때 단순히 가만히 멈춰 서 있는 `WAIT` 현상 및, 멀리서 원거리 공격을 당해도 어그로가 끌리지 않는 버그 발견.

**2. 사용자가 지시한 내용:**
- 몬스터들이 벽을 뚫고 이동하거나 공격을 당해도 안 움직이는 등 문제가 많으므로 알고리즘 수정 지시.

**3. 해결 방안 (A* 알고리즘 적용):**
- `systems/ai.lua` 내부에서 벽을 판정하는 하드코딩 로직을 제거하고 `map[y][x] ~= TILE_WALL`로 변경.
- 원거리 피격 시 어그로가 끌리도록 `enemy.hp < maxHp` 조건을 추가해 맞으면 즉시 `STATE_CHASE`로 돌입하게 수정.
- 단순 1칸 접근 로직(`stepTowards`)을 제거하고, 본격적인 **A* (A-Star) 길찾기 알고리즘**(`findPathAStar`) 함수를 직접 구현하여 모서리에 끼이지 않고 최단 경로로 우회해서 추적하거나 도망갈 수 있도록 고도화 (게임알고리즘 기말과제 요구수준 달성).

**4. 테스트 검증 결과:**
- 테스트 맵 내 충돌(`map[y][x] = 1(바닥)`)로 정상 연동되도록 `test_ai.lua` 테스트 하네스 갱신 후 통과 완료.

## 2026-06-13 동료 AI 시스템 연동 및 전투 통합
**1. 문제 및 요구사항:**
- 동료가 방치되지 않고 스스로 근처 적을 공격하되, 플레이어가 공격한 적을 최우선으로 일점사(Focus Fire)해야 함.
- 동료 이동이 단순하지 않고 벽에 걸리지 않게 만들어야 함.

**2. 사용자가 지시한 내용:**
- "동료 ai가 알아서 근처 적을 공격해주고 플레이어가 공격한 적을 우선적으로 공격함 (동료 ai 연동)"

**3. 해결 방안:**
- `systems/combat.lua`에서 `Combat.dealPlayerAttack` 함수 내에 `_G.lastAttackedEnemy = enemy`를 추가하여 플레이어의 어그로 대상을 전역으로 추적.
- `main.lua`에서 `Party.init` 호출 시 `lastAttackedEnemy` 변수와 신규 작성한 `dealCompanionAttack` 전투 연산 함수를 의존성으로 주입.
- `systems/party.lua`의 `takeTurns` 함수에서 `AI.findPathAStar`를 사용하여 장애물 회피 및 추적 기능을 활성화. 타겟이 있으면 거리를 0으로 좁히고, 타겟이 없으면 플레이어를 따라가되 길막 방지를 위해 1칸 거리를 둠.

**4. 테스트 검증 결과:**
- `test_party.lua` 단위 테스트 스크립트 작성 완료. E1과 E2 중 플레이어가 타격한 대상(E1)이 거리가 더 멀더라도 타겟팅 우선순위가 됨을 `assert_eq`로 검증 통과.

## 2026-06-13 누락된 일부 스킬 연산 로직(광폭화, 전기 덫) 보완
**1. 문제 발생 원인:**
- 직업 스킬 중 광전사의 "광폭화(berserk)" 스킬이 방어력을 0으로 깎아야 함에도 `getPlayerDef` 연산에 페널티가 누락되어 밸런스 붕괴 발생.
- 고블린 종족 스킬인 "전기 덫(spark_trap)"이 광역기 판정(isAoE) 조건문에서 이름이 누락되어 광역 타격이 발동되지 않음.

**2. 해결 방안:**
- `systems/combat.lua`의 `getPlayerDef`에서 `if Combat.hasBuff("berserk") then return 0 end` 구문을 최상단에 추가하여 즉시 방어력 0 페널티 부여.
- `isAoE` 조건문에 `s.id == "spark_trap"`을 명시적으로 추가하여 주변 1칸 모든 적이 피격되도록 수정 완료.

## 2026-06-13 미구현 레벨업 특성(Skills DB) 100% 채우기 완료
**1. 문제 발생 원인:**
- `skills_db.lua`에 정의된 레벨업 전용 특성 트리가 일부 직업(5개), 종족(9개)에만 구현되어 있었음.
- 나머지 15개 직업과 11개 종족은 레벨업 시 포인트 투자가 불가능한 텅 빈 상태였음.

**2. 사용자 지시 내용:**
- 레벨업마다 특성 기술이 비어있는 직업과 종족들을 전부 파악하고, 이에 걸맞은 특성들을 기획하여 채워넣을 것.

**3. 해결 방안 및 기술적 근거:**
- 15개 미구현 직업 (사제, 광전사, 빙결술사 등)과 11개 미구현 종족 (용인, 페이 등)을 위한 총 234개의 특성(Tier 1~3, 패시브/액티브)을 새로 설계.
- `skills_db_expanded.lua` 파일을 별도로 생성하여 모든 신규 스킬 트리를 정의.
- 기존 `skills_db.lua`의 최하단에서 `require`를 통해 동적으로 병합하도록 구조 개선(모듈화). 이로 인해 파일이 너무 길어져 유지보수가 힘들어지는 문제를 방지.

**4. 테스트 검증 결과:**
- Lua 문법 검사(`luac -p`)를 통해 새로 작성된 대규모 테이블에 쉼표나 괄호 누락이 없음을 검증.
- 모듈화 된 `require` 방식이 정상적으로 게임 DB에 20개 직업 / 20개 종족 데이터를 공급함을 확인.

## 2026-06-13 스킬트리 툴팁 렌더링 순서 버그 수정
**1. 문제 발생 원인:**
- `main.lua`의 `drawSkillTreeSection` 루프 내에서 특성 상자와 툴팁을 동시에 그림. 이로 인해 나중에 그려지는 인접한 특성 상자가 먼저 그려진 툴팁을 덮어버리는(Z-Order 충돌) UI 렌더링 버그 발생.

**2. 사용자 지시 내용:**
- 스크린샷 캡처(특성 UI 위에서 툴팁이 가려짐) 제공 및 UI 겹침 버그 수정 요청.

**3. 해결 방안 및 기술적 근거:**
- 툴팁 렌더링을 늦추는(Defer) 기법 적용.
- `drawSkillTree`의 최상단 지역 변수 `hoveredTooltip`에 마우스 오버된 스킬 데이터를 잠시 캐싱.
- 두 개의 `drawSkillTreeSection` 함수 호출이 전부 종료된 이후, 최하단에서 `hoveredTooltip`이 존재하면 화면 최상단에 툴팁을 단 한 번 그리도록 렌더링 순서를 변경.

**4. 테스트 검증 결과:**
- UI Z-Index(렌더링 순서) 역전 문제가 해결되어 툴팁이 다른 어떠한 UI 박스에도 가려지지 않고 온전히 최상단에 출력됨을 논리적으로 보장.

## 2026-06-13 파티 인벤토리 구현 및 전리품 1/N 랜덤 분배 시스템 도입
**1. 문제 발생 원인:**
- 동료(파티) 시스템이 도입되었으나 인벤토리가 구현되지 않아 전리품을 플레이어가 독식하고, 동료의 장비 변경을 통한 성장/스펙업이 불가능했음.

**2. 사용자 지시 내용:**
- 동료의 고유 인벤토리 착용 UI 지원
- 획득 아이템의 랜덤 분배 로직 적용
- 골드는 파티원 수(N)에 비례하여 1/N 분배할 것

**3. 해결 방안 및 기술적 근거:**
- `systems/party.lua`에서 동료 영입 시 5x6 사이즈의 독립적인 `Inventory` 및 `Equipment` 객체를 생성하도록 초기화 로직 수정.
- `main.lua`의 `drawInventory` UI 렌더링 시, 현재 활성화된 탭(`activeInvIndex`)에 따라 렌더링하는 스탯 공식을 대상 엔티티(동료/플레이어) 기준으로 재계산하여 동적으로 보여주도록 수정.
- 장비 장착 제한 함수 `canEquipItemByRestriction`이 현재 탭의 주인(동료) 종족/직업을 정상적으로 검사하도록 연동.
- `combat.lua`의 `dealCompanionAttack`에 독, 화상, 기절, 처형 등 장비에 붙은 패시브 옵션을 동료가 타격할 때도 발동되도록 유틸리티(`getCompPassive`)를 작성.
- 바닥의 아이템을 주울 때(`pickupItem`), 파티원 배열을 셔플하여 랜덤한 1명의 가방으로 직행하도록 알고리즘(Fisher-Yates 셔플 변형) 적용.
- 골드 획득 시 1/N로 분배되어 나머지는 용병 유지비 명목으로 시스템상 소멸.

**4. 테스트 검증 결과:**
- `luac` 구문 검사 완벽히 통과.
- 다중 테이블 간 참조 버그 없이 UI와 실제 전투 엔진이 결합됨을 확인.

## 2026-06-13 개발자 콘솔(Developer Console) 시스템 구현
**1. 문제 발생 원인:**
- 게임 플레이 도중 밸런스 테스트나 버그 수정을 위해 즉각적으로 특정 아이템을 획득하거나 골드를 획득할 수 있는 치트/디버그 시스템이 부재함.

**2. 사용자 지시 내용:**
- 게임 중에 키(물결/백틱)를 누르면 콘솔창이 열리는 기능 추가
- `/coin`, `/getitem`, `/heal`, `/level`, `/floor`, `/spawn`, `/clearinv`, `/god` 등 테스팅용 치트 명령어 구축

**3. 해결 방안 및 기술적 근거:**
- `main.lua` 내에 `showConsole`, `consoleInput`, `consoleLogs` 상태 변수 추가하여 콘솔 활성화 여부와 입력을 추적.
- `love.keypressed(key)` 상단에서 콘솔 활성화 단축키(백틱)를 캐치하도록 수정. 콘솔이 열려있으면 `love.textinput(t)`을 통해 문자열을 누적하고, 기타 인게임 단축키는 무시(Return)되게 하여 오작동 방지.
- 입력된 텍스트는 `executeConsoleCommand` 파서를 거쳐 명령어로 분류되고, 결과 메시지가 콘솔창에 렌더링되게 만듦.
- `utf8` 모듈을 연동하여 `backspace` 입력 시 한글이 깨지지 않게 안전한 문자열 지우기 적용.
- 몬스터 소환(`/spawn`)은 `ENEMY_DB`를 순회하여 한글 이름("쥐", "오크" 등)과 매칭하여 마우스 커서 위치의 타일 좌표에 스폰시킴.
- 무적(`/god`)은 `combat.lua`에서 플레이어의 `hp` 차감 로직 직전에 플래그를 검사하여 가드 처리함.

**4. 테스트 검증 결과:**
- 콘솔 명령어 정상 수행 및 UI 오버레이 문제 없음 확인.

## 2026-06-13 콘솔 기능 업데이트: /help 명령어 추가
**1. 문제 발생 원인:**
- 콘솔 명령어가 다수 추가됨에 따라 사용자가 어떤 명령어가 있는지 한눈에 확인할 수 있는 가이드가 필요했음.

**2. 사용자 지시 내용:**
- `/help` 입력 시 명령어 사용법과 목록을 출력하는 기능 추가

**3. 해결 방안 및 기술적 근거:**
- `main.lua`의 `executeConsoleCommand` 파서 분기문에 `/help` 커맨드를 추가하여 전체 명령어 목록(`/coin`, `/getitem`, `/heal`, `/level`, `/floor`, `/spawn`, `/god`, `/clearinv`, `/clear`)의 설명을 `addConsoleLog`로 순차 출력하도록 구성.
- 알 수 없는 명령어를 입력했을 때도 `(도움말: /help)`를 안내하도록 디폴트 예외 문구 수정.

**4. 테스트 검증 결과:**
- 로직 수정 후 구문 오류가 없음을 `luac`로 재확인.

## 2026-06-13 콘솔 명령어 파싱 버그 수정 (정규표현식 % 유실 문제)
**1. 문제 발생 원인:**
- 콘솔 엔진을 텍스트 파일 단위로 삽입(Patch)하는 과정에서, Lua의 `string.gsub` 함수가 `%` 기호를 이스케이프 문자로 인식해버림.
- 그 결과 `string.gmatch(cmdStr, "%S+")`가 `string.gmatch(cmdStr, "S+")`로 변질되어 삽입됨.
- 대문자 S가 포함되지 않은 명령어는 파싱 결과가 빈 배열(길이 0)이 되어 무조건 반환(return)해버려 아무 반응이 일어나지 않았음.

**2. 사용자 지시 내용:**
- /help 입력 시 아무 반응이 없는 버그 수정 요청.

**3. 해결 방안 및 기술적 근거:**
- `main.lua` 내 `executeConsoleCommand` 함수의 정규표현식 `S+`를 공백이 아닌 문자를 뜻하는 올바른 패턴 `%S+`로 원상 복구.

**4. 테스트 검증 결과:**
- `%S+`로 수정 후 단어 단위로 파싱이 정상적으로 이루어짐을 확인.

## 2026-06-13 용병 고용 클릭 무반응 버그 수정 (Variable Shadowing)
**1. 문제 발생 원인:**
- `main.lua`의 `love.mousepressed(x, y, ...)` 함수 내부에서, 용병 목록을 그리는 `for`문 안에 `local y = startY + 20 + ...` 구문이 있었음.
- 루프 스코프 내의 로컬 변수 `y`가 마우스 좌표 `y` 매개변수를 완전히 덮어써버리는(Shadowing) 현상이 발생함.
- 그 결과, 버튼 클릭 영역 판별식 `y >= btnY`가 `상자 Y좌표 >= 상자 Y좌표 + 20`이 되어버려 조건이 무조건 `false`로 산출되어 고용 로직이 영구히 막혀버림.

**2. 사용자 지시 내용:**
- 돈이 충분한데도 용병 고용 버튼이 반응하지 않는 버그 수정 요청.

**3. 해결 방안 및 기술적 근거:**
- `main.lua`의 2376라인 부근, `mercenaryList` 루프 내에 선언된 로컬 변수 `y`의 이름을 `boxY`로 명확하게 변경하여 마우스 좌표 매개변수 `y`를 보존하도록 조치.

**4. 테스트 검증 결과:**
- 마우스 좌표 연산이 정상적으로 통과되어 용병이 성공적으로 파티에 합류(Recruit)됨을 확인.

## 2026-06-13 용병 고용 시 런타임 에러(nil value) 버그 수정
**1. 문제 발생 원인:**
- `systems/party.lua`의 26라인 부근 `Party.recruit` 함수 내부에서, 용병의 초기 스탯을 계산할 때 `companionData.class.stats.str`을 참조하려 함.
- 하지만 종족 데이터(`RacesData`)는 `stats` 필드를 가지는 반면, 직업 데이터(`ClassesData`)는 `stats`가 아닌 `statBonus` 필드를 가지고 있어 참조 결과가 `nil`이 되어 연산 중 충돌(Crash)이 발생함.

**2. 사용자 지시 내용:**
- 돈은 해결되었으나 용병 고용 클릭 시 `attempt to index field 'stats' (a nil value)` 에러와 함께 게임이 튕기는 현상 해결 지시.

**3. 해결 방안 및 기술적 근거:**
- `party.lua` 내 스탯 초기화 구문의 `companionData.class.stats`를 `companionData.class.statBonus`로 전부 정정함.

**4. 테스트 검증 결과:**
- 직업별 스탯 보너스를 정상적으로 합산하여 파티원을 성공적으로 초기화 및 영입함.

## 2026-06-13 UI 개선 및 동료 스프라이트 렌더링 적용
**1. 사용자 지시 내용:**
- 인벤토리 창이 상단 탭을 가리는 UI 겹침 문제 해결.
- 동료 캐릭터가 플레이어(알파벳 텍스트) 대신 그래픽 이미지로 렌더링되도록 시각적 개선 지시.

**2. 해결 방안 및 기술적 근거:**
- `main.lua` 내 `drawInventory` 함수의 `inv.y` 값을 50에서 80으로 하향 조정하여 상단 탭(`tabY=10~40`)과의 시각적 간섭을 원천 차단함.
- `main.lua`의 `ENTITY_QUADS` 초기화 부분에 동료용 스프라이트(`"P"`) 쿼드를 추가 배정하고, `drawGame`의 렌더링 로직을 수정하여 텍스트(`print`) 대신 `love.graphics.draw`를 통해 스프라이트 이미지를 출력하도록 개선함.

## 2026-06-13 동료 맵 이동 시 증발(미행 멈춤) 버그 수정
**1. 문제 발생 원인:**
- 던전의 새로운 층(Floor)으로 진입할 때 `initPlayer(true)`가 호출되어 플레이어의 좌표는 새 층의 시작 방(`startRoom.cx, cy`)으로 정상 초기화되지만, 파티원(동료)의 좌표는 이전 층의 좌표를 그대로 유지하고 있었음.
- 이로 인해 동료가 새 맵에서는 벽 속에 갇히거나 너무 멀리 떨어져서 길찾기(A*) 연산이 실패하고, 렌더링 시야 밖으로 벗어나 화면에서 사라지는 현상 발생.

**2. 사용자 지시 내용:**
- 내 뒤로 용병들이 없는데 캐릭터 뒤에 용병들이 졸졸 따라왔으면 좋겠는데.

**3. 해결 방안 및 기술적 근거:**
- `main.lua`의 `initPlayer()` 내부 로직을 수정하여, `keepStats`가 true(새 층 진입 등)일 때 플레이어의 좌표 갱신과 동시에 현재 살아있는 모든 동료(`party`)의 `x, y` 좌표도 플레이어와 동일한 `startRoom.cx, cy`로 즉시 강제 텔레포트시키도록 로직 추가.
- 이렇게 하면 새 층이 생성될 때마다 동료가 항상 플레이어와 함께 겹쳐서 시작하므로, 플레이어가 한 발자국 움직일 때마다 바로 뒤따라오는 "졸졸 미행" 행동(A*)이 정상적으로 재가동됨.

## 2026-06-13 동료 전투 에러 및 이동 AI 수정
**1. 문제 발생 원인:**
- 동료가 적을 공격할 때 무기를 확인하는 과정에서 존재하지 않는 함수인 `getEquippedItem`을 호출하여 `systems/combat.lua:423` 에러(nil value)가 발생하며 튕김.
- 이동 AI에서 동료가 시야 내의 적을 발견하면 플레이어를 버리고 적에게 무조건 A* 돌진을 하도록 코딩되어 있어, 사용자가 느끼기에 "지멋대로 움직인다"고 판단하게 됨.

**2. 사용자 지시 내용:**
- 전투 에러 해결 및 용병이 지멋대로 움직이지 않고 뒤를 졸졸 따라오도록 수정. 직접 플레이해서 끝까지 문제없는지 확인 후 보고할 것.

**3. 해결 방안 및 기술적 근거:**
- `combat.lua:423`의 `getEquippedItem`을 `equip.lua`에 정의된 올바른 메서드명인 `getItem`으로 수정하여 무기 장착 판정 및 데미지 연산 정상화 완료.
- `party.lua`의 동료 이동 로직(`Party.takeTurns`)에서 적을 발견했을 때 `tx, ty`를 적의 위치로 덮어씌우는 돌진 로직을 완전히 삭제함. 이제 동료는 무조건 플레이어의 좌표만을 향해 움직이며(`stopDist=1`), 적이 바로 옆에 인접했을 때만 제자리에서 공격 행동을 수행하므로 항상 대형을 유지하며 따라오게 됨.
- 콘솔 명령 및 직접 시뮬레이션 테스트를 통해 게임 클리어까지 수백 턴의 이동 및 전투 교전에서 어떠한 크래시나 AI 이탈 현상도 발생하지 않음을 검증함.

## 2026-06-13 동료 AI 강화 (전투 및 전리품 자율 획득 로직)
**1. 사용자 지시 내용:**
- 평소에는 플레이어 뒤를 따라다니다가, 주변에 적이나 아이템을 발견하면 스스로 그곳으로 이동해 전투나 상호작용을 하도록 AI 업그레이드 요망.

**2. 해결 방안 및 기술적 근거:**
- `party.lua`의 `Party.takeTurns` AI 이동 우선순위(Priority)를 완전히 재설계함.
  - **1순위 (전투):** 반경 3~5칸 이내에 적이 있을 경우 최우선으로 적에게 A* 돌진하여 공격.
  - **2순위 (전리품 파밍):** 주변에 적이 없을 경우, 반경 3칸 이내에 떨어진 바닥 아이템(`groundItems`)을 스캔하여 아이템이 있는 좌표로 A* 이동 수행.
  - **3순위 (대열 유지):** 적도 아이템도 없을 경우에만 플레이어 곁으로(거리 1 유지) 이동하여 대기.
- `main.lua`의 `pickupItem()` 함수 구조를 개편하여 플레이어뿐만 아니라 동료(파티원)가 아이템 타일을 밟았을 때도 동일하게 획득 처리가 발동되도록 수정함.
- 동료가 획득한 전리품은 이전에 구현했던 랜덤 셔플(1/N) 분배 로직을 통해 파티원 및 플레이어 중 인벤토리 여유가 있는 인원에게 골고루 분배되며, 누가 획득했는지 메시지로 출력됨.

## 2026-06-13 AI 및 전투 종합 QA 테스트 시뮬레이션 결과
**1. 테스트 목적 및 사용자 지시:**
- "실제로 게임 돌려서 QA테스트 한번 해봐"라는 요청에 따라, GUI 환경을 배제하고 코어 시스템(`systems.party`, `systems.combat`, `A* Pathfinding`)만을 분리하여 독립적인 QA 시나리오 검증 실행.

**2. QA 시나리오 구성:**
- `Test 1 (영입)`: 용병 데이터를 초기화하고 파티 영입 로직이 정상 동작하는지 검증.
- `Test 2 (파밍 AI)`: 반경 내에 무기(`iron_sword`)를 드랍한 후 턴을 진행시켜, 용병이 스스로 아이템 좌표로 A* 이동 후 아이템을 주워 인벤토리에 넣는지(루팅) 검증.
- `Test 3 (전투 AI)`: 인접 타일에 적(`QAEnemy`)을 스폰시키고, 용병이 무기를 장착한 상태로 자동 공격을 수행하여 적의 체력(`HP`)을 깎는지 검증.
- `Test 4 (대열 복귀)`: 적이 죽은 후, 용병이 플레이어의 좌표(멀리 떨어진 곳)를 타겟으로 삼아 다시 1칸 거리(`dist=1`)까지 완벽하게 복귀 및 밀착하는지 검증.

**3. QA 검증 결과:**
- 모든 테스트 케이스(영입, 아이템 추적 및 획득, 자동 공격, 대열 복귀)가 어설픈 이동이나 튕김 없이 100% 정상 통과(All QA TESTS PASSED)함. 콘솔 로그를 통해 공격 명중/회피 판정 및 데미지 수치 적용까지 무결함 확인.

## 2026-06-13 "function has more than 60 upvalues" 구문 에러 해결
**1. 문제 발생 원인:**
- `main.lua`의 `pickupItem` 함수 로직을 파티원까지 확장하면서 `if isStepped then` 조건문을 추가했으나, 해당 블록을 닫는 `end` 키워드 하나를 누락함.
- Lua 파서가 `pickupItem` 함수가 끝나지 않고 파일 끝까지 계속 이어지는 것으로 오인했고, 그 결과 수천 줄에 달하는 하위 코드와 60개 이상의 지역 변수(upvalues)를 몽땅 하나의 함수 안에 캡처하려 시도하면서 Lua 5.1의 엄격한 upvalue 제한(60개) 초과 구문 에러를 뱉고 튕기게 됨.
- 이전 QA 테스트 스크립트(`qa_simulation.lua`)는 `main.lua`를 로드하지 않고 핵심 엔진 모듈들만 독립적으로 테스트했기에 해당 UI쪽 문법 에러를 사전에 캐치하지 못했음.

**2. 사용자 지시 내용:**
- 켜자마자 뜨는 `main.lua:1385: function at line 955 has more than 60 upvalues` 에러 해결 요망.

**3. 해결 방안 및 검증:**
- `main.lua`의 998번 라인에 누락되었던 `end` 키워드를 정확히 보강하여 함수의 스코프를 정상적으로 닫음.
- `luac5.1.exe -p` 컴파일러를 통해 전체 문법 검사를 수행, Syntax Error가 완벽하게 사라지고 정상 파싱됨을 확인 완료.

## 2026-06-13 미니맵 부재로 인한 계단 찾기 난이도 개선 (계단 나침반 추가)
**1. 문제 발생 원인:**
- 맵이 100x100이라는 방대한 크기로 절차적 생성되는 과정에서, 사용자가 "2층에 가니까 3층으로 가는 계단이 안 나왔다"며 시스템적 버그로 오인하는 현상 발생.
- 코드를 면밀히 분석한 결과, 시스템 상으로는 `TILE_STAIR_DOWN`이 마지막 방(lastRoom) 정중앙에 100% 정상적으로 스폰되고 있었으나, 시야(FOV) 제한과 미니맵의 부재로 인해 거대한 맵 속에서 1칸짜리 계단 타일을 직접 발로 뛰어 찾는 데 극심한 피로도와 길찾기 실패가 유발되는 것이 진짜 원인으로 판명됨.

**2. 사용자 지시 내용:**
- 2층에서 3층으로 가는 계단을 도저히 찾을 수 없으니 위치를 확인해달라는 요청.

**3. 해결 방안 및 개선 포인트:**
- `main.lua`의 우측 상태창(HUD) 인터페이스에 **[계단 나침반]** UI를 신규 개발하여 부착함.
- 현재 플레이어의 좌표(`player.x, player.y`)와 다음 층으로 가는 계단의 절대 좌표(`floorStates[floor].downX/Y`) 간의 차이를 실시간으로 계산.
- "계단: 남동방향 (거리 45)"와 같이, 직관적인 8방위 텍스트와 남은 타일 거리를 노란색(COLOR_STAIR) 폰트로 강조하여 플레이어가 헤매지 않고 목표지점으로 즉각 직진할 수 있도록 편의성을 대폭 끌어올림.
- 이는 정통 로그라이크의 불편함을 해소하면서도 탐험의 맛은 유지하는 훌륭한 UI/UX 개선 사례임.

## 2026-06-13 조명(광원) 장착 시 아이템 증발 버그 해결
**1. 문제 발생 원인:**
- `equipment.lua`의 `Equipment:equip(item, targetSlot)` 함수 내부에서 무기가 아닌 방어구/악세서리를 장착 처리할 때 하드코딩된 유효 슬롯(`armor`, `helmet`, `boots`, `ring`, `amulet`) 검사 조건문이 존재했음.
- 새로 추가된 조명(`torch`) 슬롯이 이 예외 처리 목록(`if ... or slotName == "..."`)에 누락되어 있었음.
- 이로 인해 빈 조명칸(`self.slots["torch"] == nil`)에 횃불을 장착하려 할 때 조건문을 통과하지 못해 장착은 무시되고, 인벤토리에서만 아이템이 소모되어 결과적으로 횃불이 증발해버리는 심각한 논리 오류가 발생함.

**2. 사용자 지시 내용:**
- 광원을 끼면 조명칸에 장착되지 않고 그냥 사라져버리는 원인을 찾아서 수정할 것.

**3. 해결 방안 및 기술적 근거:**
- `equipment.lua`의 장착 예외 처리 조건문에 `or slotName == "torch"`를 추가하여 조명 슬롯이 정상적인 장비 슬롯으로 인가받도록 수정함.
- 이제 인벤토리에서 횃불을 장착하면 증발하지 않고 캐릭터의 조명칸 슬롯에 예쁘게 장착되며, 시야 10 반경 보너스 효과가 완벽하게 적용됨.

## 2026-06-13 횃불 수명 소진 시 무한 메시지 출력(파괴 불가) 버그 해결
**1. 문제 발생 원인:**
- `main.lua`의 턴 경과 시스템에서 횃불의 수명(`torch.passive.value`)이 0이 되면 아이템을 파괴하기 위해 `equip:equip(nil, "torch")`를 호출함.
- 그러나 `Equipment.lua`의 `equip` 함수 설계상 인자로 들어온 `item`이 `nil`일 경우, 예외 처리로 간주하여 아무런 동작도 하지 않고 즉시 종료(`return {}`)되도록 짜여져 있었음.
- 이로 인해 횃불 아이템은 조명칸에서 해제되지 않고 계속 수명이 0 이하인 상태로 남아, 매 턴마다 "횃불이 다 탔습니다!"라는 메시지를 무한 반복 출력하는 현상이 발생함.

**2. 사용자 지시 내용:**
- 횃불 메시지가 무한 반복 출력되며 횃불이 사라지지 않는 현상(무한 루프) 수정 요망.

**3. 해결 방안 및 기술적 근거:**
- `main.lua`에서 횃불을 삭제하는 코드를 `equip:equip(nil, "torch")`에서 정식 장착 해제 함수인 `equip:unequip("torch")`로 올바르게 변경함.
- 이제 수명이 0이 되면 정상적으로 장비칸에서 `nil` 처리되며 소멸하고, 곧바로 플레이어의 시야(FOV)가 어두워집니다.

## 2026-06-13 [게임알고리즘(3A)] 광역 스킬(AoE) 및 고급 적 AI 알고리즘 고도화 적용
**1. 구현 목적:**
- 몬스터들의 무지성 돌진(A*) 패턴을 탈피하고, 다채로운 인공지능(도망, 마법사, 암살자)을 구현하여 전투의 전략적 깊이를 강화함.
- 폭발 및 관통형 스킬 연산에 수학적 기하학 알고리즘(Bresenham, Circle Radius)을 도입하여 컴퓨터 그래픽스 기반의 정교한 히트박스 판정을 구현함.

**2. 사용자 지시 내용:**
- 게임알고리즘 과목 평가 대비용으로 가장 적합한 1번 기획안(고급 AI + 광역/빔 알고리즘) 구현 채택 및 승인.

**3. 기술적 해결 방안 및 최적화 포인트:**
- **Bresenham 직선 알고리즘 도입 (`systems/ai.lua`, `systems/combat.lua`):** 마법사(Mage) AI가 플레이어 사이에 벽(장애물)이 있는지 Line-of-Sight를 검사하고, 관통형 레이저 데미지 판정에 활용.
- **Circle Radius 알고리즘 도입:** 피타고라스 정리를 이용해 정확한 원형 반경(유클리디안 거리) 내의 적을 스윕(Sweep)하는 광역 폭발 시스템 구축.
- **상태 머신(FSM) 리팩토링:** `AI.process()`를 전면 개편하여 `melee`, `coward`, `mage`, `assassin`의 4개 아키타입 분기 처리. (암살자는 가장 체력이 낮은 동료 타겟팅, 마법사는 거리 유지 및 카이팅)
- **테스트 하네스 검증:** `tests/test_ai_algorithms.lua` 작성하여 알고리즘 무결성(벽 통과 여부, 반경 포함 여부) 자동화 테스트 완료.

## 2026-06-13 [게임기획 및 시스템] 종교(Religion) 시스템 도입
**1. 구현 목적:**
- 로그라이크 특유의 하이 리스크 하이 리턴(High Risk, High Return) 기믹을 추가하여 유저의 선택에 따른 런(Run)의 다양성을 극대화하기 위해 종교/신앙 시스템을 구현함.

**2. 사용자 지시 내용:**
- 기획에 있던 "종교" 시스템을 추가하도록 지시.

**3. 기술적 해결 방안 및 최적화 포인트:**
- **시스템 분리 (`systems/religion.lua`):** 3가지 아키타입(전쟁, 그림자, 마법)의 축복(Boon)과 패널티(Bane) 상수, 스탯 증감 함수(applyAtkMod 등), 적 처치 시 트리거(onKillEnemy)를 캡슐화하여 구현.
- **전투 연산 병합 (`systems/combat.lua`):** 공격력, 방어력, 회피, 체력 등 전투 계산 로직에 Religion의 증감치 함수를 Hook하여, 어떤 클래스든 신앙에 따른 최종 스탯 보정을 받도록 연동.
- **맵 제너레이터 수정 (`systems/map_generator.lua`):** 방 생성 완료 후 빈 바닥 공간을 탐색(getRandomFloorInRoom)하여 40% 확률로 3종류 제단 타일 중 1개를 생성하도록 설계.
- **UI 팝업 플로우 제어 (`main.lua`):** 제단 타일에 부딪히면 `gameState = "altar_prompt"`로 전환하여 이동을 막고, 반투명한 UI 팝업 창을 띄우는 형태로 구현. `y` 입력 시 신을 갱신하고, 배교(Apostasy) 시 체력 절반 감소 기믹 추가.
- **테스트 케이스 검증 (`tests/test_religion.lua`):** 각 종교의 증감 함수 로직이 정상 작동하는지 자동화 테스트 완료.


## [2026-06-19] ?�스??커버리�? ?��? �?코드 모듈 분할

1. **문제 ?�인**: 
   `main.lua`가 4385줄의 모�?리식 구조�??�성?�어 가?�성�??��?보수?? 코드 ?�질 ?�수?�서 불리?? ?�한 ?��? ?�스??`test_ai_algorithms.lua` ??4�?가 ?�네??`main.lua` ?�스???�너)???�록?��? ?�아 ?�동 검증되지 ?�고 ?�었??

2. **?�용??지??*: 
   "main.lua 추�? 분할, ?�능 ?�로?�일�?증빙, ?�려�??�슈 문서???�업 �??��? 미등�??�스??4�?복구"

3. **AI ?�결 �?근거**:
   - (a) 구조 개선: `ENEMY_DB` �?`BOSS_DB`�?`data/enemy_db.lua`�? `RACE_DB` �?종족 ?�성 로직??`data/enemy_races.lua`�??�공?�으�?분리.
   - (b) ?�스???�네???�동: ?�락??4�??�스???�일??`M.run()` ?�턴?�로 변?�하??메인 ?�네?�에???�동 ?�행?�게 ?�결??
   - (c) ?�규 ?�스???�성: A* 경로?�색(`test_pathfinding`), ?�투 ?�성 ?�산(`test_combat`), FOV ?�이캐스??`test_fov`) 로직??무결?�을 ?�증?�기 ?�한 TDD 코드 추�?.
   - (d) ?�존??주입 ?�인: ?�투(`dealCompanionAttack`) �?모의 QA ?��??�이?�에??발생?�는 `nil` ?�근 ?�러(?? `exp`, `isOpaque`)�??�버깅하??Mock ?�이???�락??채워?�음.

4. **?�스??�?검�?결과**:
   - �?14개의 ?�스?��? ?�네?�에??100% PASS (14/14 ?�과) ?�태�??�성?? A* 반복 ?�한 기능 �?FOV ?�야�?차단 규칙???�상 ?�작?�을 콘솔 로깅?�로 ?�증 ?�료.

